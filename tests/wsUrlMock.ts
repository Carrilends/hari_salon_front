// Stub de `src/api/ws-url` para Jest (ver `moduleNameMapper` en jest.config.cjs).
// El módulo real lee `import.meta.env`, que no existe fuera del bundle de Vite.
export const WS_URL = 'http://localhost:3001';
